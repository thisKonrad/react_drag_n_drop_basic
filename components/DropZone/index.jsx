/* :::: Ingredients :::: */
//import { useState } from 'react';
import styles from './style.module.css';

export default function DropZone({ player }) {
  return (
    <section className={styles.wrap}>
      <p className={styles.title}>hello player!</p>
    </section>
  );
}
